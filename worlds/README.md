# Gazebo worlds

The boatShed world is there to match the correspondinf map in the `traethlin_mapping_navigation` package.  It is oversized because of the gazebo bug that does not allow setting a non-zero position for the heightmap (the collisions do not follow the offset).
